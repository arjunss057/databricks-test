bundle:
  name: simple-bundle

targets:
  dev:
    workspace:
      host: ${env.DATABRICKS_HOST}

resources:
  jobs:
    simple-job:
      name: simple-job
      tasks:
        - task_key: run-notebook
          notebook_task:
            notenook_path: ./test_notebook_git.ipynb
          new_cluster:
            spark_version: 13.3.x-scala2.12
            node_type_id: Standard_DS3_v2
            num_workers: 1
            
